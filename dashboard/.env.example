# Local dev: leave this file as-is (or delete it), api.js falls back to localhost.
# Production (Vercel): set this exact variable in Vercel project settings ->
# Environment Variables, using your live Railway backend URL.
VITE_API_URL=https://your-backend-url.up.railway.app
